{
  "framework": "nextjs",
  "buildCommand": "cd ../.. && npm install --no-package-lock && npm run build --workspace=@kimy/web",
  "installCommand": "npm install --no-package-lock",
  "outputDirectory": ".next"
}
